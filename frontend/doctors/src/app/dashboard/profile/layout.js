export const metadata = {
  title: "Dcotors List | Profile",
  description: "Doctors Listing App Profile Page",
};

export default function ProfileLayout({ children }) {
  return <div className="container mx-auto">{children}</div>;
}
